# mushr model

This folder contains a lightweight placeholder MuJoCo model intended for demonstration and local setup.

If you want to use a realistic `mushr` vehicle model, replace `mushr.xml` with the full vehicle description from the PRL MuSHR repository.

The current model is a minimal planar car-style body for demonstration with the `mpc_demo_mujoco.py` entry point.
